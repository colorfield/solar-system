package org.sse.ui.screen{		import org.sse.solarsystem.ManualVoyageSpringCamera;		import be.colorfield.net.LoadExternalAsset;	import be.colorfield.ui.Application;	import be.colorfield.ui.screen.IScreen;	import be.colorfield.ui.screen.ScreenAbstract;		import flash.display.Sprite;		/**	 * ECRAN chargeant un fichier précompilé comprenant le voyage manuel dans le système solaire	 */	public class Screen05Voyage extends ScreenAbstract implements IScreen	{				private var swfContainer:Sprite;				public function Screen05Voyage()		{					}				/**		 * Création des éléments et de leurs options d'affichage		 */		override public function buildElements():void		{				// TODO : l'écran 1 est en fait une position aléatoire des objets dans le système solaire			// on montre la position du vaisseau.			// Si le mode automatique est choisit, un calcul est réalisé en fonction de l'algorithme de Dijkstra			// Si le mode manuel est choisit, on passes en voyage manuel			// => utiliser une factory						// Flash n'arrive pas à compiler ceci, problème de RSL probablement			// => solution : on précompile et on charge en externe			/*			var voyage:ManualVoyageSpringCamera = new ManualVoyageSpringCamera();			var voyageOptions:Object = {x:0,y:0};			addElement(voyage,voyageOptions);				*/						swfContainer = new Sprite();			var scOptions:Object = {x:0,y:0};			addElement(swfContainer,scOptions);								}				override public function postDisplayOperations():void{			LoadExternalAsset.getInstance().loadMovie("swf/SpringCameraShader.swf",swfContainer);		}	}}